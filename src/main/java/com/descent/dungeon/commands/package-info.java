/**
 * Developer/debug commands ({@code /descent ...}, {@code /debug ...}). See
 * {@link com.descent.dungeon.commands.DescentCommands} for the full tree and
 * which subcommands are live versus stubbed pending a later phase.
 */
package com.descent.dungeon.commands;
